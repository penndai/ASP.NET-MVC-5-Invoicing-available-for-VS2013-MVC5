<span class="login">
@if(Request.IsAuthenticated) {
    <text>Hello <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log out", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Log in", "LogOn", "Account") ]
}
</span>